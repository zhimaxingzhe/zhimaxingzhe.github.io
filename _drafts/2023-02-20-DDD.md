---
layout: post
title:  "DDD 笔记"
categories: 方法论
tags:  方法论
author: zhimaxingzhe
link: https://zhimaxingzhe.github.io/
---

* content
{:toc}

# 前言
随着微服务和敏捷的快速推广，早在04年提出来的领域驱动设计（DDD）思想得以被越来越多人采纳和使用。
初学者的我认为领域驱动设计是为了解决业务和技术间间隔的好思想，技术团队通常更加专注于技术实现，对于业务的理解那是产品经理的事情，DDD要求我们深入理解业务，根据业务模型设计出贴合实际需求的系统模型，这样的系统模型实现出来的系统能更好地支撑业务发展，以至于反哺业务发展。  
那么DDD需要怎样操作呢？和业务领域专家（业务人员、产品经理甚至用户）学习业务流程，识别领域事件（业务事件）、命令、名词；即定义业务流程中发生了什么？即在何时何地谁干了什么事情，带来什么影响？进而更深层次了解业务为什么要这样做，结合业务发展趋势设计拓展性和延续性更好的系统设计。  
我所处的团队也在推广使用DDD，学习DDD势在必行。  

# 概念解释
战略设计：限界上下文、通用语言，子域

战术设计：聚合、实体、值对象、资源库、领域服务、领域事件、模块

限界上下文与通用语言
域是问题空间，限界上下文是解决空间。 

限界上下文是一个显式的语义和语境上的边界，领域模型便存在于边界之内。边界内，通用语言中的所有术语和词组都有特定的含义。

通用语言就是能够简单、清晰、准确描述业务涵义和规则的语言。

把限界上下文拆解开看。限界就是领域的边界，而上下文则是语义环境。 通过领域的限界上下文，我们就可以在统一的领域边界内用统一的语言进行交流。

上下文组织和集成模式
防腐层(Anticorruption Layer)：简称ACL，在集成两个上下文，如果两边都状态良好，可以引入防腐层来作为两边的翻译，并且可以隔离两边的领域模型。

值对象=将一个值用对象的方式进行表述，来表达一个具体的固定不变的概念。

聚合
聚合是领域对象的显式分组，旨在支持领域模型的行为和不变性，同时充当一致性和事务性边界。

我们把一些关联性极强、生命周期一致的实体、值对象放到一个聚合里。

聚合根
聚合的根实体，最具代表性的实体

应用服务

工厂

资源库
是聚合的管理，仓储介于领域模型和数据模型之间，主要用于聚合的持久化和检索。它隔离了领域模型和数据模型，以便我们关注于领域模型而不需要考虑如何进行持久化。
我们将暂时不使用的领域对象从内存中持久化存储到磁盘中。当日后需要再次使用这个领域对象时，根据 key 值到数据库查找到这条记录，然后将其恢复成领域对象，应用程序就可以继续使用它了，这就是领域对象持久化存储的设计思想。

事件模型

# 事件风暴
事件风暴是一种通过协作的方式捕获行为需求的方法，在这个过程里，业务人员和技术人员一起消化领域知识、形成统一语言、并为领域建模奠定基础。
## 识别领域事件
领域事件就是业务流程中每个步骤引发的的结果，从结果入手来梳理需求，从操作入手，更容易把业务想清楚。事件风暴中的“事件”两个字就来源于领域事件。描述业务现象，以完成时 + 被动语态描述事件。  
第一步是参加的人，各自写出领域事件；第二步是一起讨论，统一理解。这种先发散，后收敛，反复迭代的过程实际上就是一种头脑风暴。  
这里需要区分开的是技术事件不是领域事件，查询功能也不算是领域事件。
![识别场景](/image/DDD/识别场景.jpg)
## 识别命令
所谓命令（command），就是引发领域事件的操作，我们可以通过分析领域事件得到。除了识别出命令本身以外，我们通常还要识别出谁执行的命令，以及为了执行命令我们要查询出什么数据。
## 识别领域名词
这里说的领域名词，是从命令、领域事件、执行者、查询数据里找到的名词性概念。   
其实识别领域名词的最终目的是要找到领域模型中的对象。  

在事件风暴里不需要列出所有的领域事件和命令，只需要列出关注的事件和命令即可，即主要的、足以用于表达和交流领域知识的步骤；否则会让结果变得繁琐，让人抓不住重点。  

步骤的颗粒度，考虑从业务的角度，我们是把每个小步骤都当作独立的一个事务来看待，还是把它们合起来作为同一个事务。  

事件风暴主要应用在需求不清晰，或者理解不统一的情况下，通过协作的方式理清业务、达成一致，所以通常对于新项目比较适用。

事件风暴的结果可以保存为一张表格中：
| 业务流程 | 命令 | 时间 | 执行者 | 查询数据 | 领域对象|
| --- | --- | --- | --- | --- | --- |
| 产品接入提交 | 产品选择 | 时间 | 机构产品管理员 | 查询本机构产品信息 | 产品基本信息|

在事件风暴中发现的业务规则可以记录成一张领域规则表：
| 规则编号 | 模块 | 规则描述 | 举例 | 影响的主要功能 |
| --- | --- | --- | --- | --- |
| R001 | 产品接入 | 机构只能选择自己机构下的产品，填写产品参数提交接入申请 | 华夏基金的产品管理员A登录机构合作平台，输入华夏基金产品代码000001（华夏成长），输入的产品不能是招商基金的161725（招商中证白酒） | 机构产品接入新增产品功能 |

# 领域建模
主要是要识别领域对象（domain object），领域对象之间的关系，以及领域对象的关键属性，必要的时候还要将领域对象组织成模块。  
领域模型是用领域模型图来表达的，通常用 UML 来画。
## 识别实体
先假定每个领域名词都是一个实体，把它们用类的符号画出来。通过后面的分析，我们还会发现，有些名词不是实体，有些要转换成其他形式。  
在领域建模阶段，我们主要关注的是实体和它们之间的关系。
如果实体的名字已经能清晰说明实体的含义，那我们就不需要加属性了。如果名字还不足以充分表达含义，我们可以写几个关键属性，来辅助说明。
## 关联关系
1对1
1对多
多对多
自关联
抽象模型
归纳了共性，但个性却丢了，需要补充个性模型，如枚举、类别
增加约束，凡是约束，必须在程序中的某个地方进行实现
## 识别操作
## 划分模块
## 完善业务规则表
## 建立词汇表

# 数据库设计
## 数据库与领域模型一致
## DDD方法是ER图的超集

# 分层架构
![包模块划分](/image/DDD/包模块划分.webp)
## 分离领域
## application层作为门面
### 接收请求
### 领域对象的数据转换
## 适配器层
### 处理输入和输出
### 处理持久化
## 通用工具和框架

# 代码实现
## 贫血模型
## 充血模型
## 领域对象和逻辑
### 表意接口
### 领域服务
### 工厂模式
### 模块划分，先横后竖（先业务后技术）
## 代码更加面向对象
### API的封装
### 领域对象的封装
### 表意接口提高封装
### 集成消除重复


# 聚合
## 领域对象特征
## 聚合根表示法
## 派生关联
## 标识
## 对聚合进行封装
## 创建聚合
## 聚合的持久化
## 用事务保证固定规则
## 单实体聚合

# 值对象
## 同一性
## 值对象类别
## 值对象模型
## 值对象代码
## 值对象与实体的区别


# 限定
## 用法
## 作用
## 现实 

# 泛化建模
## 概念
## 隐式概念
## 何时使用泛化
## 识别泛化
### 归纳法
### 演绎法
### 泛化表达的关系
## 实现泛化
### 设计表的策略
### 设计主键策略
### 领域对象的集成或接口实现
### 仓库实现内存对象与数据库数据的转换


# 限界上下文
## 统一语言
## 康威定律
## 上下文映射
## 上下文概念映射图
## 驱动架构设计
## 微服务的设计

# CQRS
## 命令
## 查询
## 四个层面的策略
## C中的Q如何实现

# 分析模式

# 落地经验
## 切入场景
## 试点项目和团队



[《手把手教你落地DDD》](https://time.geekbang.org/column/intro/100311801)

捕获行为、领域建模、架构设计、数据路设计、代码实现

事件风暴
识别领域事件、识别命令、识别领域名词
领域事件，业务流程中发生了什么？现象，以完成时 + 被动语态描述
命令识别，谁做了什么事情，导致领域事件的发生？本质，为什么要发生专业的事情？
从而确认领域名词有哪些？
- 统一语言，从业务视角描述
- 技术事件不能当做领域事件、查询不算领域事件

主线流程：
产品被选中、产品已被录入、产品已被提交、参数被复核、已被提交审核、上架已审核、产品已上架

支线流程：
- 分润已录入、分润已提交、分润已审核、分润数据已同步
- 风控评级已发起、风控评级已完成
- 往来账户已录入、往来账户已提交、往来账户已审核、已报备监管行

领域命令：
- 准入、接入、录入、提交、审核、上架、报备
- 留痕

领域名词：找到对象
机构信息
机构用户
内部用户
产品基础信息：基本信息、交易参数、费率信息、交收参数、分成比例
往来账户
定开区间
展示信息
分润信息：发票信息、分成比例信息
参数维护：基础参数维护、特定参数维护
产品池：一级池、二级池、池产品、产品投研信息


业务流程|命令|时间|执行者|查询数据|领域对象


| 业务流程 | 命令 | 时间 | 执行者 | 查询数据 | 领域对象|
| --- | --- | --- | --- | --- | --- |
| 产品接入 | 产品选择 | 时间 | 机构产品管理员 | 查询本机构产品信息 | 产品基本信息|
| 产品接入 | 产品录入 | 时间 | 机构产品管理员 | 查询本机构产品信息 | 费率折扣、清算交收、展示配置|






UML


首先，模型是以解决特定问题为目的的。例如沙盘模型是为了卖房，而建筑图纸是为了盖楼。没有目的就谈不上模型。  
第二，模型都是对现实世界或人们思维中的事物进行的模拟。例如沙盘模型和建筑图纸都是对建筑物的模拟，而玩具车是对真车的模拟。  
第三，模型总是提取了被模拟事物中的部分信息，而忽略掉了其他大部分信息。例如，沙盘模型提取了楼盘的外观信息，但是忽略了内部结构和建筑材料信息。而建筑图纸反映了内部结构信息，但忽略了外观信息。到底提取哪些信息，忽略哪些信息，取决于模型的目的。  
第四，模型可以有多种表现形式，例如图纸、影像、公式以及电脑中的文件等等。具体采用哪种形式，取决于要解决的问题和当前的技术水平。  


第一步是捕获行为需求，研究为了满足业务需求需要有怎样的流程和功能，可以用传统的用例分析来做，也可以用咱们这里的事件风暴。  
第二步是分析，在行为需求的基础上，对现实世界中领域知识或者说业务概念进行分析和提炼，形成分析模型。尽管分析模型反映的仍然是业务概念，然而是经过抽象得来的，更加严谨、精练和一致，因此比行为需求更加接近实现。  
第三步是设计，在分析模型的基础上，增加技术关注点，进行架构设计、详细设计、数据库设计等，形成设计模型。设计模型介于分析模型和代码之间。  
第四步是编写代码。代码经过编译后，就成为了运行在机器上的“机器模型”了。  


DDD 的领域模型则是业务视角（原来的分析模型）和技术视角（原来的设计模型）的交集。它反映了业务人员和技术人员的共识。